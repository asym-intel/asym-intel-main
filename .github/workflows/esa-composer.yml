name: "European Strategic Autonomy Monitor Composer"
# Sprint CD — thin caller. Reusable body: _reusable-composer.yml
on:
  workflow_dispatch:
    inputs:
      model:
        description: 'Override composer model (optional; blank = use metadata.yml default)'
        required: false
        default: ''
  workflow_run:
    workflows: ["European Strategic Autonomy Monitor Reviewer"]
    types: [completed]
jobs:
  call:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/_reusable-composer.yml
    with:
      slug: european-strategic-autonomy
      applier_workflow: esa-applier.yml
      model: ${{ inputs.model || '' }}
    secrets: inherit
