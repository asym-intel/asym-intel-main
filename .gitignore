# Hugo build output
# NOTE: /docs/ is intentionally tracked — GitHub Pages serves from it.
# Stage 6b GitHub Actions will automate builds. Until then, commit manually.

/public/
/resources/_gen/
/assets/jsconfig.json

# Hugo cache
.hugo_build.lock

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Node (if using Hugo pipes with npm)
node_modules/
package-lock.json

# SPEC-ENGINE-PROMPT-ARCHIVAL v1.0.3 §7.5 — engine exchange logs must never be committed here.
# (Authoritative home is asym-intel-internal/archive/exchanges/; these paths are local/engine-only.)
pipeline/incidents/prompt-exchanges.jsonl
pipeline/incidents/*.exchange.jsonl
archive/exchanges/
archive/exchanges/_push-errors/
